use chrono::{DateTime, FixedOffset, Duration, Utc, Local};

struct Habit {
    name: String,
    date_begin: Option<DateTime<Local>>,
    date_end: Option<DateTime<Local>>,
}

impl Habit {
}

fn main() {
}
